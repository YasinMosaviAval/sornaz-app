import 'package:flutter/material.dart';
import '../components/audio_list.dart';
class MusicPlayerTabs extends StatelessWidget {
  const MusicPlayerTabs({super.key});
  @override
  Widget build(BuildContext context) => const AudioList();
}
